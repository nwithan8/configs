This is a placeholder for config files that HAVE to exist on GitHub for various reasons.
The files in this repo are actually submodules stored elsewhere on GitHub, which themselves are read-only mirrors of config repos stored on Codeberg.

Please do NOT reference files via this repo if possible, and instead use the GitHub (or even better, the Codeberg) submodule repos directly.
